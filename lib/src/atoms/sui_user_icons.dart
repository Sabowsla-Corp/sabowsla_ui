import 'package:flutter/widgets.dart';

class SUIUserIcon extends StatelessWidget {
  const SUIUserIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
