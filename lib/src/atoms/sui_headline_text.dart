import 'package:flutter/material.dart';

class SUIHeadlineText extends StatelessWidget {
  const SUIHeadlineText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}